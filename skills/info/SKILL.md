---
name: info
description: Open the current sponsor's link in your browser
disable-model-invocation: true
allowed-tools: Bash
---

The command below has already run and opened the current sponsor in the browser.
Do not read files, list directories, or run any other tools. Reply with exactly
this one line and nothing else:

Opened the sponsor in your browser.

!`"${CLAUDE_PLUGIN_ROOT}/bin/adtention" open`
